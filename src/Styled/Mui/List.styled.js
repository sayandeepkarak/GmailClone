import styled from "styled-components";
import { ListItem, ListItemButton } from "@mui/material";

export const AllUsers = styled(ListItem)``;

export const ListButton = styled(ListItemButton).attrs({
  sx: { padding: "14px 28px" },
})``;
