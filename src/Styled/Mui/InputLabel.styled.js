import styled from "styled-components";
import { InputLabel } from "@mui/material";

export const SimpleLabel = styled(InputLabel).attrs({
  sx: {
    fontSize: "14px",
    fontWeight: "bold",
    minWidth: "20%",
  },
})``;
