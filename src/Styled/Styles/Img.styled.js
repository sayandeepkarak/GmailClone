import styled from "styled-components";

export const LogoText = styled.img.attrs({
  alt: "x",
})``;

export const MenuListIcons = styled.img.attrs({
  alt: "x",
})`
  opacity: 0.71;
`;

export const AddOneIcons = styled.img.attrs({
  alt: "x",
})`
  opacity: 1;
  width: 24px;
`;
